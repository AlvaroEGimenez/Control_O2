<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/action_update"
        android:title="@string/actualizar"
        app:showAsAction="ifRoom"
        android:icon="@drawable/ic_edit_white_24dp">
    </item>

    <item android:id="@+id/send_to_provider"
        android:title="@string/enviar_recarga"
        app:showAsAction="never"
        android:visible="true">
    </item>

    <item android:id="@+id/action_add_observ"
        android:title="@string/editar_observaciones"
        app:showAsAction="never">
    </item>

</menu>